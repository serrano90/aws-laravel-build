# AWS Laravel Build
This project is docker image with Amazon Web Services CLI and PHP Composer.

The image is usefull for building and deploy php aplication to AWS Lambda using GitLab CI

## DockerHub
The docker hub repository is `https://hub.docker.com/r/serrano90/aws-laravel-build/`

```
docker pull serrano90/aws-laravel-build
```